apiVersion: apps/v1
kind: Deployment
metadata:
  name: websocket-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: websocket-app
  template:
    metadata:
      labels:
        app: websocket-app
    spec:
      containers:
      - name: websocket-app
        image: python:3.11-slim
        command: ["python3", "-m", "uvicorn", "ws_main:app", "--host", "0.0.0.0", "--port", "80"]
        ports:
        - containerPort: 80
        volumeMounts:
        - name: ws-code
          mountPath: /app
      volumes:
      - name: ws-code
        configMap:
          name: ws-code
